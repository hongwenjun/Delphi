unit ThoughtWorks.QRCode.Codec.Util.Color;

interface

type
  TColor_Fields = (GRAY = $AAAAAA, LIGHTGRAY = $BBBBBB, DARKGRAY = $444444,
    BLACK = $000000, WHITE = $FFFFFF, BLUE = $8888FF, GREEN = $88FF88, LIGHTBLUE
    = $BBBBFF, LIGHTGREEN = $BBFFBB, RED = $FF8888, ORANGE = $FFFF88, LIGHTRED = $FFBBBB);

  IColor = interface
  end;

implementation

end.

